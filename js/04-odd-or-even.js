// loop to interate from 0 to 15:

for (let i = 0; i < 16; i++) {
  if ((i % 2) === 0) {
    console.log(`${i} is even`);
  } else {
    console.log(`${i} is odd`)
  }
}

// this code is saying: as long as i is divisible by 2 with none remaining,
// it is an even number, print even to the console.